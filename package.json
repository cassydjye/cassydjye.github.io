{
  "name": "seedjye-portfolio",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "check": "tsc --noEmit",
    "clean": "rm -rf .next out node_modules",
    "install:clean": "rm -rf node_modules .next && npm ci --no-audit --no-fund",
    "preview:static": "python3 -m http.server 3000 --directory out"
  },
  "dependencies": {
    "next": "16.2.10",
    "react": "19.2.7",
    "react-dom": "19.2.7"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "typescript": "^5.9.0"
  },
  "overrides": {
    "postcss": "8.5.19"
  },
  "engines": {
    "node": ">=22 <23",
    "npm": ">=10"
  }
}
